// Placeholder test — the default counter test is no longer valid
// after replacing main.dart with the MaterialWeibo app entry point.
//
// Real widget / integration tests should be added per-feature.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    expect(1 + 1, equals(2));
  });
}
